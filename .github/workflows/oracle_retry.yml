name: Oracle Cloud Free Tier Auto-Retry

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

concurrency:
  group: oracle-vm-claim
  cancel-in-progress: true

permissions:
  contents: read
  actions: write

jobs:
  claim-vm:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Auto-Retry Script
        id: retry
        env:
          SINGLE_RUN: "true"
          OCI_CONFIG_JSON: ${{ secrets.OCI_CONFIG_JSON }}
          OCI_API_KEY_PEM: ${{ secrets.OCI_API_KEY_PEM }}
        run: |
          python main.py

      - name: Trigger Next Run (Continuous Auto-Loop)
        if: always() && steps.retry.outcome == 'success'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -f /tmp/vm_created ]; then
            echo "🎉 VM created or already exists! Stopping auto-loop."
          else
            echo "🔄 VM not created yet. Dispatching next workflow run..."
            sleep 30
            gh workflow run oracle_retry.yml --repo ${{ github.repository }}
          fi
